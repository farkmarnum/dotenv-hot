# Minimal example using TS

## Demo

Run `yarn install-dotenv-hot` and then `yarn watch-env`.

Then, in another terminal, run `yarn start`.

Modify `.env` (in parent directory) and see the value update live.
